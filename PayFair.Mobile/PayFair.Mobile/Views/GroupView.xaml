<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PayFair.Mobile.Views.GroupView">

    <StackLayout Orientation="Vertical">
        
        <StackLayout Orientation="Vertical">
            
            <StackLayout Orientation="Horizontal">

                <Button Text="Back" Command="{Binding BackCommand}" HorizontalOptions="Start"/>
                <Label Text="{Binding GroupName}" HorizontalOptions="CenterAndExpand"/>
                <Button Text="Settings" Command="{Binding SettingsCommand}" HorizontalOptions="End"/>

            </StackLayout>

            <StackLayout Orientation="Horizontal">
                <Label Text="Total" HorizontalOptions="StartAndExpand"/>
                <Label Text="Total" HorizontalOptions="CenterAndExpand"/>
                <Label Text="Total" HorizontalOptions="EndAndExpand"/>
            </StackLayout>
            
        </StackLayout>
        
        <StackLayout>


            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </StackLayout>
</ContentPage>